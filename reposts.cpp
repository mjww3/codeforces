#include <stdio.h>
#include <iostream>
#include <math.h>
#include <algorithm>
#include <set>
#include <queue>
#include <string>
#include <vector>
using namespace std;

int main()
{
	int n;
	cin>>n;
	vector<string> s;
	while(n--)
	{
		string h;
		cin>>h;
		s.push_back(h);
	}

	vector<vector<string> > graph;
	graph.push_back("mukul");

}